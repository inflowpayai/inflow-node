---
'@inflowpayai/mpp': patch
'@inflowpayai/mpp-buyer': patch
'@inflowpayai/mpp-seller': patch
---

Require mppx 0.8.12 or newer and emit MPP receipts with `challengeId` and nested `settlement.amount` and
`settlement.currency` fields.
