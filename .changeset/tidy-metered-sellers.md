---
'@inflowpayai/x402': minor
'@inflowpayai/x402-seller': minor
---

Add explicit seller-side `upto` selection using advertised Permit2 capabilities and the foundation EVM scheme server.
Keep exact and balance defaults, load the optional EVM peer only for metered registrations, and document measured
settlement through the foundation adapter's `setSettlementOverrides` API.
