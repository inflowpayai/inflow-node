# @inflowpayai/mpp

## 0.5.0

### Minor Changes

- Initial release: the shared MPP definitions for InFlow's `inflow` payment method — the `inflow` `Method` namespace (defaulting to `charge`), the MPP wire types, the RFC 8785 JCS + base64url codec and `WWW-Authenticate: Payment` header render/parse, the InFlow MPP REST client, protocol constants, and typed errors.
